package telran.numbers;

public class TictactoeGame {
	/**
	 * 
	 * @param matrix square rows == columns(3 or more)
	 * @param nRow(number of row)
	 * @param nColumn - number of column
	 * @param symb - being filled symbol, x or o
	 * @return 0 - game is not over, 1- game over, 2 - game over without winner
	 */
	public static int tictactoeMove(char matrix[][], int nRow, int nColumn, char symb) {
		return -1;
	}

}
